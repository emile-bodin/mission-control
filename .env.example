# Host ports
FRONTEND_PORT=3000
BACKEND_PORT=8000

# PostgreSQL
POSTGRES_DB=bodin
POSTGRES_USER=bodin
# Placeholder only. Replace in local .env before exposing PostgreSQL.
POSTGRES_PASSWORD=replace-with-local-password
GOOGLE_CALENDAR_ICS_URL=

# Pulse (read-only inventory/status source)
PULSE_BASE_URL=
PULSE_API_TOKEN=
